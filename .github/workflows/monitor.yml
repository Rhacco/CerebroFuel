name: Crypto Signal Monitor

# v3.3.3: mixed entry/exit opportunities, exact public interval-volume confirmation,
# 3h-base/late-entry guards, +3/+5% target memory and BTC market-quality reference.
on:
  workflow_dispatch:
    inputs:
      send_discord:
        description: "Bericht an Discord senden"
        required: true
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: crypto-signal-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Repository laden
        uses: actions/checkout@v5

      - name: Python einrichten
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Lokalen Tag bestimmen
        id: context-key
        shell: bash
        run: echo "day=$(TZ=Europe/Berlin date +%F)" >> "$GITHUB_OUTPUT"

      - name: Neuesten Roh- und Tageskontext laden
        id: daily-cache
        uses: actions/cache/restore@v5
        with:
          path: .cache/seasonality
          key: seasonality-v333-${{ steps.context-key.outputs.day }}-${{ github.run_id }}
          restore-keys: |
            seasonality-v333-${{ steps.context-key.outputs.day }}-
            seasonality-v333-
            seasonality-v332-${{ steps.context-key.outputs.day }}-
            seasonality-v332-
            seasonality-v331-${{ steps.context-key.outputs.day }}-
            seasonality-v331-
            seasonality-v330-
            seasonality-v327-complete-weeks-r2-

      - name: Letzte Vollpool-Snapshots laden
        uses: actions/cache/restore@v5
        with:
          path: .cache/flash
          key: flash-v333-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            flash-v333-
            flash-v332-
            flash-v331-
            flash-v330-
            flash-v329-

      - name: Gelernten Zielausgang laden
        uses: actions/cache/restore@v5
        with:
          path: .cache/opportunity
          key: opportunity-v333-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            opportunity-v333-

      - name: Abhängigkeiten installieren
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Kompatiblen Tageskontext vorbereiten
        id: prepare-daily
        env:
          LCW_API_KEY: ${{ secrets.LCW_API_KEY }}
          SEND_DISCORD: "false"
          LCW_RATE_STATE_PATH: .cache/lcw-rate.json
        run: python main.py --daily-only --no-send

      - name: Aktualisierten Tageskontext speichern
        if: steps.prepare-daily.outputs.changed == 'true'
        uses: actions/cache/save@v5
        with:
          path: .cache/seasonality
          key: seasonality-v333-${{ steps.context-key.outputs.day }}-${{ github.run_id }}

      - name: Frische Analyse ausführen
        env:
          LCW_API_KEY: ${{ secrets.LCW_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SEND_DISCORD: ${{ inputs.send_discord }}
          LCW_RATE_STATE_PATH: .cache/lcw-rate.json
        run: python main.py --monitor-only

      - name: Aktualisierte Vollpool-Snapshots speichern
        if: always() && hashFiles('.cache/flash/state.json') != ''
        uses: actions/cache/save@v5
        with:
          path: .cache/flash
          key: flash-v333-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Gelernten Zielausgang speichern
        if: always() && hashFiles('.cache/opportunity/state.json') != ''
        uses: actions/cache/save@v5
        with:
          path: .cache/opportunity
          key: opportunity-v333-${{ github.run_id }}-${{ github.run_attempt }}
