# v3.6.2 active Lighter runtime uses only the Python standard library.
# Keep requests available for the retained v3.5 compatibility modules.
# Package revision: v3.6.2-top5-context-r1
requests>=2.31,<3
